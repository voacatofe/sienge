# PostgreSQL Client Authentication Configuration File
# ===================================================
# Development environment configuration

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (Unix domain socket)
local   all             all                                     trust

# IPv4 local connections
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5

# Allow connections from Docker network
host    all             all             172.16.0.0/12           md5
host    all             all             192.168.0.0/16          md5

# Allow connections from host machine (Windows/Mac Docker Desktop)
host    all             all             host.docker.internal/32 md5

# Allow all connections (development only - DO NOT USE IN PRODUCTION)
host    all             all             0.0.0.0/0               md5